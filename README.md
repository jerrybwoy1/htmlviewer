# htmlviewer

Current live release: **DEBOOGER2000 BUILD011**.

The root `index.html` reconstructs the verified Build011 release, verifies its SHA-256 before rendering, and uses the Build010 approved release only as the verified base for the Build011 delta.

Build011 final SHA-256: `5336273da42ea2754b5ad2ce4dd7cff649fb9557a2841c30963723a309a20811`
